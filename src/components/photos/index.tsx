import * as React from 'react';

class UploadedPhotos extends React.Component {
    public render(): React.ReactNode {
        return (
            <div />
        )
    }
}

import PhotoDrop from './drop'
import PhotoPreview from './preview'

export { PhotoDrop, PhotoPreview, UploadedPhotos }